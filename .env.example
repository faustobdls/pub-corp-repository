# Flask settings
DEBUG=True
SECRET_KEY=dev-key-change-in-production
HOST=0.0.0.0
PORT=5000

# Storage settings
STORAGE_TYPE=local  # 'gcp' or 'local'

# GCP settings (used when STORAGE_TYPE=gcp)
GCP_BUCKET_NAME=pub-corp-repository
GCP_PROJECT_ID=your-project-id

# Local storage settings (used when STORAGE_TYPE=local)
LOCAL_STORAGE_DIR=./storage

# Pub.dev settings
PUB_DEV_URL=https://pub.dev

# Cache settings
CACHE_TIMEOUT=3600  # 1 hour by default